'use strict'

const redis = require('redis')
const tap = require('tap')



tap.test('redis data structures', t => {
  t.test('redis hash data structure', t => {
    t.equal(1, 1)
    t.end()
  })
  t.end()
})
